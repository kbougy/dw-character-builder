class CharacterClass {
    constructor(className, damage, hp, drives, backgrounds, bonds) {
        this.className = className;
        this.damage = damage;
        this.hp = hp;
        this.drives = drives;
        this.backgrounds = backgrounds;
        this.bonds = bonds;
    }
}
